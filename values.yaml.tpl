deployments:
  mds-agency:
    version: ${AGENCY_VERSION}
  mds-attachment-backend:
    version: ${ATTACHMENT_BACKEND_VERSION}
  mds-audit-backend:
    version: ${AUDIT_BACKEND_VERSION}
  mds-audit:
    version: ${AUDIT_VERSION}
  mds-policy:
    version: ${POLICY_VERSION}
  mds-policy-backend:
    version: ${POLICY_BACKEND_VERSION}
  mds-compliance:
    version: ${COMPLIANCE_VERSION}
  mds-daily:
    version: ${DAILY_VERSION}
  mds-jurisdiction:
    version: ${JURISDICTION_VERSION}
  mds-jurisdiction-backend:
    version: ${JURISDICTION_BACKEND_VERSION}
  mds-policy-author:
    version: ${POLICY_AUTHOR_VERSION}
  mds-geography:
    version: ${GEOGRAPHY_VERSION}
  mds-geography-author:
    version: ${GEOGRAPHY_AUTHOR_VERSION}
  mds-geography-backend:
    version: ${GEOGRAPHY_BACKEND_VERSION}
  mds-ingest-backend:
    version: ${INGEST_BACKEND_VERSION}
