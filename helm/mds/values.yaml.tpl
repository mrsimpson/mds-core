apis:
  mds-agency:
    image: ${AGENCY_IMAGE}
  mds-audit:
    image: ${AUDIT_IMAGE}
  mds-policy:
    image: ${POLICY_IMAGE}
  mds-compliance:
    image: ${COMPLIANCE_IMAGE}
  mds-daily:
    image: ${DAILY_IMAGE}
  mds-jurisdiction:
    image: ${JURISDICTION_IMAGE}
  mds-jurisdiction-service:
    image: ${JURISDICTION_SERVICE_IMAGE}
  mds-policy-author:
    image: ${POLICY_AUTHOR_IMAGE}
  mds-web-sockets:
    image: ${WEB_SOCKETS_IMAGE}
  mds-geography:
    image: ${GEOGRAPHY_IMAGE}
  mds-geography-author:
    image: ${GEOGRAPHY_AUTHOR_IMAGE}
